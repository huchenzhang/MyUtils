<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.huchenzhang.myutils">

    <!--网络权限-->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- 读取IMSI的权限,IMEI -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <!-- 外部存储读权限，调试工具加载本地补丁需要 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <!--百度语音合成权限相关-->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:name=".SophixStubApplication"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!--fabric-->
        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="4f0e7eb35bb87be9875b1eec420f6c227faa7528" />

        <activity android:name=".netUtils.NetUtilsActivity"/>

        <activity android:name=".xuliehao.XuLieHao"/>

        <activity android:name=".swiperefresh.SwipeRefreshActivity"/>

        <activity android:name=".recyclerview.MyRecyclerView"/>

        <activity android:name=".rxjava.RxJava"/>

        <activity android:name=".myhttp.OkHttp"/>

        <activity android:name=".myDataBinding.MyDataBindingActivity"/>

        <activity android:name=".yuyin.YuYinActivity"/>

        <activity android:name=".keepalive.HooliganActivity"
                  android:configChanges="keyboardHidden|orientation|screenSize|navigation|keyboard"
                  android:excludeFromRecents="true"
                  android:exported="false"
                  android:finishOnTaskLaunch="false"
                  android:launchMode="singleInstance"
                  android:theme="@style/HooliganActivityStyle"/>

    </application>

</manifest>